package com.yash.projectManagement.test;

import static org.junit.jupiter.api.Assertions.assertEquals;

import java.time.LocalDate;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import com.yash.projectManagement.entity.Project;
import com.yash.projectManagement.serviceimpl.ProjectServiceImpl;

class ProjectTest {
	
	/*
	 * ProjectServiceImpl service; Project project;
	 * 
	 * @BeforeEach void setUp() throws Exception { service=new ProjectServiceImpl();
	 * project=new Project(6, "AAA", "AAA", true, LocalDate.now()); }
	 * 
	 * @Test void testAddProject() { assertEquals(true, service.add(project)); }
	 * 
	 * @Test void testGetProject() { assertEquals("SLB", service.get(3).getName());
	 * }
	 * 
	 * @Test void testGetAllProject() {
	 * 
	 * assertEquals(3, service.getAll().size()); }
	 * 
	 * @Test void testUpdateProject() { Project p=service.get(3); p.setName("AAA");
	 * p.setId(3); assertEquals(true, service.update(p, 3)); }
	 * 
	 * @Test void testDeleteProject() { assertEquals(true, service.delete(2)); }
	 */
}
